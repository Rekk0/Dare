# Dare — 进度

> **跨会话交接文件。** 新会话从这里读起，就知道现在在哪、下一步做什么、哪些坑已经踩过。
> 每完成一个里程碑或做出一个决策，就更新这里。

**最后更新**：2026-09-02（M0 完成、M1 完成）

---

## 现在在哪

**阶段**：M0 与 M1 已完成并验证
**下一步**：M2 · 可见性 + 数据层 + 活动生命周期
**待你做的事**：① 手机上真机验证 Spike C 手势并定可见窗口数值；② 提供一个 Gemini API key 才能跑 Spike A
**代码生成方式**：codex 子代理，由 Claude 拆任务、发指令、收结果、审代码

---

## 文档地图

| 文件 | 管什么 | 状态 |
|---|---|---|
| [project-design.md](project-design.md) | 产品机制、数据模型、奖励与保密规则 | v0.4 定稿 |
| [DESIGN.md](DESIGN.md) | 视觉规范、BUSTED 分镜、反 AI 味清单 | v1.0 定稿 |
| [ARCHITECTURE.md](ARCHITECTURE.md) | 技术栈、目录、模块契约、10 条不变量 | v1.0 定稿 |
| [IMPLEMENT-PLAN.md](IMPLEMENT-PLAN.md) | M0–M7 里程碑与验收标准 | v1.0 定稿 |
| 可交互视觉稿 | https://claude.ai/code/artifact/0fd381b0-68f0-46ed-a923-cbd5d7e16595 | — |

---

## 里程碑状态

| # | 里程碑 | 状态 | 备注 |
|---|---|---|---|
| M0 | 地基与技术验证 | 🟡 大部分完成 | Spike B 通过；Spike C 代码通过、待真机；Spike A 缺 key 阻塞 |
| M1 | 结算引擎（纯函数） | ✅ 完成 | 19 个测试全绿，含 10000 组 property test |
| M2 | 可见性 + 数据层 + 生命周期 | ⬜ 未开始 | |
| M3 | AI 层 + 任务预审 | ⬜ 未开始 | |
| M4 | 任务卡 + 上传 + 证据评审 | ⬜ 未开始 | 第一个能玩的版本 |
| M5 | 猜测 + 被识破 | ⬜ 未开始 | |
| M6 | 公投 + 揭晓 + 结算 | ⬜ 未开始 | 完成即 Phase 1 完成 |
| M7 | 开源化收尾 | ⬜ 未开始 | |

---

## M0 / M1 完成记录

**技术栈实际落地**：Next.js **16.3.4**（不是计划里写的 15）+ Tailwind v4 + vitest 4 + fast-check 4。
本机**没有 docker**，M2 起数据库用 PGlite 替代，生产仍是真 Postgres。

**Spike B 字体子集化 — 通过。**
得意黑 2568 KB → **8.0 KB**（0.31%），63 个字零掉字。
`scripts/subset_fonts.py` + `fonts/display-charset.txt`（显式字符清单，新增 display 文案要同步更新，否则掉字）。
脚本内置 100 KB 上限，超了直接报错 —— 超上限说明 display 字体被误用到正文上了。

**Spike A 厂商能力实测 — 阻塞。** 没有任何 AI 厂商的 API key。
这个 spike 的结论决定 Phase 1 要不要写 ffmpeg 降级管线，越早跑越好。

**Spike C 双手势揭示 — 代码通过，真机待验。**
浏览器实测逐字窗口 **3/7/7/7/5 个字**，命中设计目标 7。

**M1 结算引擎 — 通过。** 19 个测试，含 10000 组 property test 断言 I1 与 I2。

## 决策日志

按时间倒序。**只记结论和「为什么」，不记过程。**

### 2026-09-02（实现期）

**审 codex 产出时抓到一个可套利的规则漏洞：执行者猜自己领的任务。**
你知道自己的任务原文。如果知道自己完不成、公投必然过不了（拿 0 份），
就去猜自己 → 把自己搞成 busted → 以第一名领 0.5 份，**把 0 变成 0.5**。
已在 `isVoidedHit()` 里连同「出题人猜自己出的题」一起作废。
注意：property test 生成过自猜的用例但**抓不到它** —— 那 0.5 是从同一个池子里出的，
守恒仍然成立。这是规则漏洞不是守恒漏洞，只能靠人审。

**守恒的分母是任务数，不是人数。**
原实现和文档都写成 N（人数）。有旁观者（未出题者仍可投票和猜）时人数 > 任务数，
不变量就断了。正确表述：`Σ payouts + Σ forfeited === assignments.length`。

**React 里绝不能对同一个属性既写 JSX 又 setAttribute。**
父组件重渲染时 React 会把 JSX 里的值刷回 DOM，把命令式设的状态冲掉。
表现为第一次点击没反应、长按时黑条不消失。这类属性只命令式设置，
让 CSS 的无属性默认态（已涂黑）作为初始值。

### 2026-09-02（设计期）

**任务卡两种揭示手势，不是一种**
按住 = 全显（防「什么时候看」，你挑安全时机，松手即没）；
点一下 = 逐字显示逐字渐隐（防「被瞟一眼」，任何瞬间只有约 7 字在屏上）。
两者防的不是同一个威胁，互补。
**播放中再点 = 急停**，这是必需品不是优化——长按可以松手，打字机没有急停就是安全漏洞。
关键参数是可见窗口 `DWELL/STEP ≈ 7 字`：露太多等于显示半句，露太少要反复重放、用户干脆改用长按。
**必须真机调。**
减弱动效下**保留时序只去渐变**——逐字的时序是安全机制不是装饰。

**视觉母题定为整块涂黑，不是逐词涂黑**
只盖关键词会留下句式骨架（「在包厢里，让 ██ 的人主动唱一首 ██」），邻座扫一眼就能补全。
**部分防窥等于没有防窥。** 这是机制底线，不是视觉偏好。
连带否掉：用 blur 代替涂黑（截图放大可还原，且骨架仍可读）、点击切换（必须按住）。

**配色否掉「近黑底 + 酸性绿」**
那是当前 AI 生成设计最集中的聚类之一，一眼能认出来。
改用靛蓝黑 `#0E1220` + 荧光记号笔黄 `#FFE94A`，来自「机密文件 + 荧光笔」这个跟产品直接相关的世界。

**BUSTED 用闪光灯过曝，不用红色爆炸**
你是被拍到了，不是被炸了。过曝是「被识破」的物理隐喻。

**被识破立即通知本人，不再瞒到 settled**
原规则（v0.2）的前提是被猜中只扣一半、继续做还有钱拿，所以要瞒着。
v0.3 改成一票否决后前提消失——他已经归零，没有可保护的利益，继续瞒只是让人白干两小时。
**但识破者身份仍严格保密到 `settled`**，否则当场变复仇局。
副作用（他知道了就不传证据）用 BUSTED 卡上的「为面子继续」路径接住。

**奖励模型：每题 1 份独立奖池，总支出恒 ≤ N 份**
被猜中一票否决，那 1 份由前三名猜中者按 0.5 / 0.3 / 0.2 瓜分。
早期版本赏金是凭空增发的（最坏 2.5N 份），创建者会在不知情下承诺付不起的账。

**默认 provider 选 Gemini**
图 / 音 / 视频全原生，Phase 1 可以完全不写 ffmpeg 降级管线，省掉整个项目最重的一块。
**但这依赖 M0 Spike A 的实测结果**——若实测不支持视频，媒体方案当场改。

**一人一题（假设 A1）**
分配退化成 derangement，随机重试即可（期望 2.7 次）。一人多题要上二分图匹配，非 MVP。

---

## 已知的坑

**移动端长按天生难做。** 系统的文本选择、放大镜、右键菜单、滚动手势都会来抢。
必须 `contextmenu` preventDefault + `user-select: none` + `-webkit-touch-callout: none` + `touch-action: none`。
**真机验证，模拟器不算。**

**得意黑全量超过 5MB。** 必须子集化，否则移动端首屏无法接受。

**猜测接口是提示注入的正面战场。** 三条硬约束（project-design.md §5.8）必须全实现，
最有效的一条是：判定只在服务端跟阈值比较，前端只收布尔值。

**`rationale` 泄露 = 白送答案。** 它里面必然包含任务原文。用 TS 类型在响应层面杜绝，不靠自觉。

---

## Phase 1 做完必须回答的问题

不是「做完了」，是这 7 个问题有答案（详见 project-design.md §8）：

1. 真跟朋友开一局，全流程无阻断跑完了吗？
2. 有人的任务被猜中了吗？（没有 = 配额或阈值定错了）
3. 公投结果和 AI 报告分歧大吗？（分歧大 = 报告没提供有效信息）
4. 有人吐槽任务太难吗？（= 预审 `feasibility` 阈值不准）
5. 识破率多少？（过高 = 大面积归零，体验挫败）
6. 多少份被作废了？（作废多 = 参数太紧）
7. 0.5 / 0.3 / 0.2 的梯度够不够刺激「早猜早得」？

---

## 当前阻塞

**① Spike A 缺 API key。** 需要一个 Gemini（或其他多模态厂商）的 key 才能实测能力矩阵。
这个结论决定 Phase 1 要不要写 ffmpeg 抽帧 + ASR 降级管线，是最该早知道的一件事。

**② Spike C 需要真机。** 移动端长按跟系统的文本选择、放大镜、右键菜单、滚动手势打架，
模拟器和内置浏览器都不算数。

跑起来：`pnpm dev -- -H 0.0.0.0`，手机连同一个 Wi-Fi 打开 `http://<本机IP>:3000`。
页面上带 step / dwell 两个滑杆，**可见窗口是唯一靠手感定的参数**，
调出舒服的数值告诉我，我改进 `DEFAULT_STEP_MS` / `DEFAULT_DWELL_MS`。

**已知的量具问题**：本机内置浏览器的 `getComputedStyle` 返回陈旧值，
读不出内联样式的变化。验证 Redacted 的状态要读 `element.style`，不要读计算样式。
