# Dare — 视觉设计规范 v1.0

> 2026-09-02 · 配合 [project-design.md](project-design.md) 使用
> 本文是**给 codex 的实现契约**，不是灵感板。每条都是可执行的约束。

---

> **可交互视觉稿：** https://claude.ai/code/artifact/0fd381b0-68f0-46ed-a923-cbd5d7e16595

## 0. Design Read

> 移动端派对游戏**产品 UI**（不是落地页），面向 18–30 岁中文用户。
> 视觉母题是**整块涂黑的机密文件**；靛蓝黑底 + 荧光记号笔黄单一强调；
> 倾向 native CSS / Tailwind v4 + 得意黑做 display + Motion 物理动效。

### 母题：整块涂黑

整套视觉从产品机制里长出来，不是贴上去的：**任务默认被黑条盖满，长按 800ms 擦开，松手瞬间盖回。**

**必须整块盖，绝不能只盖关键词。** 留着「在包厢里，让 ██ 的人主动唱一首 ██」这样的骨架，
邻座扫一眼就能补全——句式本身已经把答案空间压到极小。**部分防窥等于没有防窥。**
这是机制底线，不是视觉偏好。

同时解决三件事：① 防肩窥，邻座只看到黑块，零信息；② 节奏，每次查看都要做一次「现在看安不安全」的判断；
③ 识别度，一整页涂黑的机密文件比零星涂黑更标志性，不靠图形 logo 也认得出。

**Dials：`DESIGN_VARIANCE 7 / MOTION_INTENSITY 8 / VISUAL_DENSITY 3`**

| Dial | 值 | 理由 |
|---|---|---|
| VARIANCE | 7 | 派对调性要有个性，但它是产品 UI，暗光下必须一眼看懂，不能为不对称牺牲可读性 |
| MOTION | 8 | **BUSTED 那一下就是产品本身**。动效不是装饰，是核心情绪节点，动机充分（§4 动效动机表） |
| DENSITY | 3 | KTV 暗光、3 秒注意力、防肩窥。一屏一件事 |

### 三条不可协商的场景约束

1. **暗光环境**：KTV / 酒吧 / 夜间。**Dark-only**，不做浅色模式。屏幕亮度是安全问题，不是偏好问题。
2. **防肩窥**：邻座随时能瞟到。任务内容默认打码，长按才显示。全局无通知、无 toast、无震动（BUSTED 除外）。
3. **单手 + 3 秒**：站着、拿着酒杯、背着人操作。所有主操作在拇指区（屏幕下 1/3）。

---

## 1. 色彩

**一个强调色（荧光记号笔黄），一个语义警报色（红），此外全是中性。**

```css
:root {
  /* 中性 — 靛蓝黑，不用纯黑，留深度 */
  --ground:  #0E1220;   /* 页面底 */
  --surface: #161B2E;   /* 卡片面 */
  --raised:  #1F263C;   /* 抬升面 */
  --line:    #2E3654;   /* 分隔线 / 描边 */
  --dim:     #6C7699;   /* 次要文字 */
  --body:    #AAB3D0;   /* 正文 */
  --bright:  #EEF1FA;   /* 主文字，不用纯白 */

  /* 涂黑条 — 比页面底更深，才盖得住 */
  --redact:  #05070E;

  /* 强调 — 荧光记号笔黄。全局唯一强调色 */
  --mark:      #FFE94A;
  --mark-deep: #D9C321;              /* 按下态 */

  /* 语义警报 — 仅用于被识破 / 危险确认 */
  --alarm: #FF3B30;

  /* 语义进账 — 仅用于结算页拿到份额 */
  --gain:  #4ADE80;
}
```

**规则：**

- **强调色锁定**：记号笔黄是全站唯一强调色。按钮、进度、选中态、擦开后的高亮，全用它。
- **黄底一律配 `--ground` 深色文字**，对比度约 15:1。**绝不在黄上放白字**，完全读不出来。
- **红只在两个地方出现**：被识破、不可逆操作确认。**绝不用红做装饰**。它出现 = 出事了。
- **绿（`--gain`）只在结算页**表示进账。不用于任何交互态。
- 无渐变文字、无外发光、无毛玻璃、无 AI 紫。
- 阴影一律染底色（`rgb(5 7 14 / .7)`），不用纯黑投影。

> **为什么不是「近黑底 + 酸性绿」**：那是目前 AI 生成设计最集中的聚类之一，一眼就能认出来。
> 记号笔黄配靛蓝黑同样年轻、同样适合暗光，但来自「机密文件 + 荧光笔」这个跟产品直接相关的世界。

---

## 2. 字体

两款中文字体，都**免费商用、可自托管**，不走 Google Fonts `<link>`。

| 用途 | 字体 | 来源 | 说明 |
|---|---|---|---|
| Display（大标题、BUSTED、数字） | **得意黑 Smiley Sans** | [atelier-anchor/smiley-sans](https://github.com/atelier-anchor/smiley-sans)，SIL OFL | 紧缩 + 微斜，天生派对/音乐节气质。这款是整个视觉的性格来源 |
| 正文 / UI | **阿里巴巴普惠体 3.0** | [puhuiti.taobao.com](https://puhuiti.taobao.com)，免费商用 | Regular / Medium / Bold / Heavy 齐全，屏显干净 |
| 等宽数字 | 普惠体 `font-variant-numeric: tabular-nums` | — | 份额、倒计时、配额用它，不引第三方 mono |

**不用 Inter，不用 Noto Sans SC 做 display**（太中性，杀掉性格）。得意黑负责情绪，普惠体负责可读。

```
Display  : 得意黑, 60–120px, letter-spacing: -0.02em, line-height: 0.92
H1       : 普惠体 Heavy, 28px / 1.2
Body     : 普惠体 Regular, 16px / 1.7, max-width: 34ch（中文按字数不按 ch 也行）
Caption  : 普惠体 Regular, 13px / 1.5, --ink-400
Number   : 普惠体 Bold, tabular-nums
```

**中文排版注意：** 得意黑本身带倾斜感，**不要再叠 `font-style: italic`**（合成斜体会毁字形）。强调用字重，不用混排另一款字体。

---

## 3. 形状与材质

- **圆角单一制**：卡片 `16px`，按钮 `full`（胶囊），输入框 `12px`。全站遵守，不混。
- **卡片只在有真实层级时用**。列表用 `divide-y border-ink-600` 或纯负空间分组。
- **不用毛玻璃**。暗光场景里 backdrop-blur 只会降低对比度和帧率，收益为零。
- 噪点颗粒：可选，仅限 `position: fixed; inset: 0; pointer-events: none` 的伪元素，**绝不加在滚动容器上**。

---

## 4. 动效

**每个动画必须能一句话说清它在传达什么。** 说不清的删掉。

| 动效 | 传达什么 | 强度 |
|---|---|---|
| **BUSTED 揭示** | 状态突变 + 情绪冲击。**这是产品的核心节点** | 满 |
| 任务卡长按揭示 | 反馈：你正在做一个有风险的动作 | 中 |
| 猜测结果三档反馈 | 状态：命中 / 接近 / 不接近 | 中 |
| 结算份额入账计数 | 叙事：一份一份数出来，制造收获感 | 中 |
| 分配倒计时 | 层级：把注意力拉到即将发生的事 | 低 |
| 列表进场 stagger | 层级 | 低 |

**禁止**：无限循环的呼吸/闪烁、装饰性 marquee、滚动劫持、自定义光标、卡片 hover 倾斜（移动端根本没有 hover）。

**技术约束：**

- 用 Motion（`motion/react`）。只动 `transform` 和 `opacity`。
- 连续值（长按进度、拖拽）用 `useMotionValue` / `useTransform`，**绝不用 `useState`**。
- 弹簧默认 `{ type: "spring", stiffness: 260, damping: 22 }`。
- **全部动效必须 `useReducedMotion()` 降级**。BUSTED 在降级模式下直接显示终态，不做冲击动画，但**内容一字不少**。

---

## 5. 核心界面：BUSTED 揭示屏

> 这是整个应用做得好不好的分水岭。其他屏都可以平庸，这一屏不行。

### 5.1 触发时机

用户被识破后，**下一次打开任务卡时**触发。不推送、不打断——他自己点进来才炸。这一点很重要：主动查看时被炸，比被通知打断更有戏剧性，也不会在他跟朋友说话时把屏幕暴露出去。

同一用户只全屏播放**一次**，之后任务卡进入常驻的 busted 静态样式。

### 5.2 分镜（总时长 1.9s）

| 时间 | 画面 | 说明 |
|---|---|---|
| 0 – 200ms | **全黑**。原任务卡内容瞬间消失 | 制造停顿。突然的"什么都没有"比突然的"很多东西"更吓人 |
| 200 – 320ms | **闪光灯过曝**：白屏闪两下，第二下更弱 | **你是被拍到了，不是被炸了。** 这是被识破的物理隐喻 |
| 200 – 340ms | 红色径向从中心铺开（`--alarm` 到透明，68%） | 闪光之后留下的警报底 |
| 260 – 480ms | **暴露了** 三个大字砸入。`scale 1.75 → 0.94 → 1`，overshoot 弹簧 | 得意黑，占屏宽 80% |
| 300ms | 同步：`navigator.vibrate([40, 30, 60])` + 屏幕抖动 3 帧（±5px，120ms） | 全应用唯一允许震动的地方 |
| 500 – 700ms | `BUSTED` 小字字距拉开淡入 | 普惠体 Bold，`letter-spacing: .52em`，`--alarm` |
| 700 – 1100ms | 说明正文逐行淡入上浮 | 情绪之后才给信息 |
| 1100 – 1400ms | 两个按钮从底部滑入（拇指区） | |
| 1400ms+ | 红色底褪到 22% 不透明度，静止 | 情绪落下来，痕迹留着 |

### 5.3 文案（照抄，不要改写成 AI 腔）

```
暴露了

BUSTED

你的任务已经被人猜中。
这一份，没了。

是谁猜中的，结算时揭晓。

[ 为面子继续 ]        [ 知道了 ]
```

- **主标题用中文不用英文**。「暴露了」对中文用户的冲击远大于「BUSTED」。BUSTED 降为副标题，负责派对游戏的调性。
- **「这一份，没了。」**——短句、句号、不解释。别写成「很遗憾，您本次任务的奖励份额将不予发放」。
- **不告诉他是谁**（§3.1 可见性规则），并且明确告诉他"结算时揭晓"，把悬念留住。

### 5.4 两个按钮

| 按钮 | 层级 | 行为 |
|---|---|---|
| **为面子继续** | 次要（描边，`--ink-050` 文字） | 保留证据上传入口。任务标记 `honor_only`，证据照传照评审，进揭晓环节，**不给钱**。这条路径是为了接住"他知道了就不传了、揭晓环节没内容"的副作用 |
| **知道了** | 主要（记号笔黄实心，`--ground` 文字） | 关闭，回活动主页。任务卡进入 busted 静态样式 |

> **按钮对比度检查**：记号笔黄 `#FFE94A` 上放 `--ground` 深色文字，对比度 ≈ 15:1，远超 WCAG AA。**绝不在黄上放白字**。

### 5.5 之后的常驻任务卡样式

不再全屏。任务卡加：红色左边框 4px、内容区降到 40% 不透明度、右上角一枚静态 `已暴露` 标签（红底深字，非闪烁）。原任务文字**仍可长按查看**——他还得完成它才有面子分。

---

## 6. 其他关键屏

### 6.1 任务卡（正常态）

一屏一张卡，垂直居中。默认状态：**任务正文被一叠黑条盖满，一个字不露**。

- 黑条按行长排布（宽度 100% / 88% / 46% 之类），视觉上就是一份被涂黑的机密文件。
- 卡片外始终可见的只有不涉密的信息：倒计时（tabular-nums）、上传按钮、猜测入口。
- **无返回键以外的导航**。这一屏就是全部。

#### 两种揭示手势

同一张卡两个手势，**防的不是同一个威胁**，互补不重复：

| 手势 | 行为 | 防什么 |
|---|---|---|
| **按住 800ms** | 全文显示，松手瞬间盖回（无过渡） | 防「什么时候看」。你自己挑安全时机，松手立刻没。快，但一瞬间全在屏上 |
| **点一下** | 逐字显示、逐字渐隐 | 防「被瞟一眼」。任何瞬间只有约 7 个字在屏上，邻座扫一眼拿到的是碎片 |
| **播放中再点** | 立刻全清 | **急停**。有人凑过来时的唯一出路 |

#### 打字机模式的参数

```
STEP  出字间隔  110ms   （出现过渡 80ms，快）
DWELL 停留时长  800ms
FADE  渐隐时长  420ms   （慢，快进慢出）

可见窗口 = DWELL / STEP ≈ 7 个字（约一个词组）
30 字任务全程 ≈ 4.1 秒
```

**可见窗口是这个机制唯一的关键参数。** 露太多（15 字以上）等于显示半句，机制失去意义；
露太少读不完要反复重放，用户干脆改用长按，模式就死了。**必须在真机上调，不能拍脑袋。**

#### 实现约束

- **急停是必需品不是优化**。长按遇到有人凑过来，松手就没了；打字机没有急停就是安全漏洞。
- **不能按字符切，要按书写单元切**。`Intl.Segmenter` 取字素簇，再把连续的 ASCII 字母数字合并成一个单元——
  `API` 一个字母一个字母冒出来是坏的。字素簇同时正确处理 emoji 和组合字符。
- **布局从第一帧就是终态**。全文一次性写进 DOM，每个单元包一个 `<span>` 只动 `opacity`。
  逐字 append 会让容器不断重排、文字跳动。
- **减弱动效降级：保留时序，只去掉渐变**（`transition: none`，硬切）。
  逐字的时序是**安全机制**不是装饰，不能因为用户开了减弱动效就把全文一次性显示出来。
- **无障碍**：屏幕阅读器无法感知定时视觉揭示。长按是无障碍路径（激活时朗读全文），
  打字机是纯视觉的可选模式。
- 屏蔽长按唤起的系统菜单：`contextmenu` preventDefault + `user-select: none` +
  `-webkit-touch-callout: none` + `touch-action: none`。
- **禁止**：部分涂黑、模糊代替涂黑（`blur` 能被截图放大还原，且骨架仍然可读）、
  点击切换成常显（点击必须是打字机或急停，不能是 toggle 全显）。

### 6.2 上传屏

进页面直接是相机取景器，不是菜单。底部三个切换：`照片 / 录音 / 录像`。

- 拍完**不显示缩略图预览**，只有一个极小的记号笔黄对勾，1.2s 后自动退回任务卡。
- 上传进度不做百分比数字，做一条 2px 的顶部进度条。安静。

### 6.3 猜测屏

选一个人 → 输入框（限 80 字）→ 提交。

结果三档，各自一套视觉，**不给数字**：

| 结果 | 视觉 |
|---|---|
| `hit` | 记号笔黄满屏闪一下 + 「**猜中了**」+ 「你是第 N 个」+ 本次赏金份额 |
| `close` | 橙黄描边脉冲一次 + 「**很接近**」 |
| `cold` | 灰色，无动效 + 「**不是这个**」 |

剩余配额始终显示在顶部：`3 / 3` 用 tabular-nums，用掉一个就划掉一个。

### 6.4 结算屏

**这是第二重要的屏**。顺序讲一个故事，不要一次性铺开：

1. 逐个揭晓每个人的任务和结果（可左右滑）
2. **识破关系图**：谁猜中了谁，用连线动画一条条画出来
3. 最后是份额榜：每人的份数从 0 计数上去（`--gain` 绿）。被识破的人在名字后挂一枚红色 `已暴露` 标签，数字本身转灰——**红只留给被识破的那一刻**，榜单上不再用红
4. 底部一行小字：本场共 N 份，发出 X 份，作废 Y 份

---

## 7. 反 AI 味检查清单（交付前逐条过）

- [ ] **零 em-dash（`—`）**。全站任何可见文字都不出现，用普通连字符
- [ ] **任务内容整块涂黑，绝无部分涂黑、绝无用 blur 代替涂黑、绝无点击切换（必须按住）**
- [ ] 暗色单一主题，无浅色模式，无中途反色
- [ ] 强调色只有记号笔黄一个；红只出现在被识破和危险确认；绿只出现在结算进账
- [ ] 圆角单一制（卡 16 / 按钮 pill / 输入 12），全站一致
- [ ] 按钮对比度：黄底配深色字，**绝无黄底白字**
- [ ] 无 Inter，无 Noto Sans SC 做 display；得意黑不叠合成斜体
- [ ] 无毛玻璃、无外发光、无渐变文字、无 AI 紫
- [ ] 无装饰性小圆点、无章节编号 eyebrow（`01 / 任务`）、无滚动提示
- [ ] 无 div 拼的假截图、无手绘 SVG 图标（用 Phosphor）
- [ ] 无无限循环动效；每个动画对得上 §4 的动机表
- [ ] 所有动效有 `useReducedMotion()` 降级，BUSTED 降级后内容完整
- [ ] `min-h-[100dvh]` 而非 `h-screen`（iOS 地址栏）
- [ ] 空 / 加载 / 错误三态齐全（加载用骨架屏，不用转圈）
- [ ] 主操作全在拇指区（屏幕下 1/3）
- [ ] 文案自审：无「很遗憾您的」「为您呈现」这类客服腔，无 AI 生造的文艺句

---

## 8. 技术落点（给 codex）

```
样式    Tailwind v4，色值走 CSS 变量（见 §1），不用 @apply 堆样式
动效    motion/react。GSAP 不引入 — 本项目没有滚动叙事需求
图标    @phosphor-icons/react，strokeWidth 全局 1.5
字体    自托管 woff2 + font-display: swap。得意黑做子集化（全量 5MB+，必须裁）
组件    shadcn/ui 打底，但必须改掉默认圆角/配色/字体，不允许原样使用
```

**字体子集化是必须做的**：得意黑全量超过 5MB，移动端不可接受。用 `fonttools pyftsubset` 按实际用字裁剪，display 字体只需要几十个字（「暴露了」「猜中了」「很接近」等固定文案 + 数字）。正文用普惠体的动态子集或 CJK 分包。
