# 参与贡献

感谢贡献。请保持改动小而明确，并遵循以下规则：

- `src/core/` 必须是无 IO 的纯函数。改动这里时必须同时提交测试。
- 结算和可见性是错了会很惨的两处。改动时请额外仔细检查边界和泄露风险。
- 不要使用 em-dash。注释和测试名写中文。
- 提交前运行 `pnpm test` 和 `pnpm build`。两者检查的文件集不同，都要通过。

## 许可

本项目采用 GNU AGPL v3。**提 PR 即表示你同意你的贡献以同样的协议发布。**
不接受来源不明的代码，以及从不兼容协议的项目里搬过来的代码。
